# Database (not required for static build)
# DATABASE_URL="postgres://username:password@localhost:5432/originary"

# NextAuth.js (not required for static build)
# NEXTAUTH_URL="https://your-domain.vercel.app"
# NEXTAUTH_SECRET="your-super-secret-jwt-secret-here-generate-a-long-random-string"

# Optional: Add your payment webhook secrets here
# RAZORPAY_WEBHOOK_SECRET="your-razorpay-webhook-secret"

# Razorpay Configuration
# IMPORTANT: Only KEY_ID goes in frontend (public)
# KEY_SECRET must NEVER be exposed in client code

# Razorpay Key ID (Public - safe to use in frontend)
# Get this from Settings → API Keys in Razorpay Dashboard
NEXT_PUBLIC_RAZORPAY_KEY_ID="rzp_live_YOUR_KEY_ID_HERE"

# Razorpay Key Secret (Private - server-side only!)
# WARNING: Never expose this in frontend code
# Only use for server-side payment verification
# RAZORPAY_KEY_SECRET="your_key_secret_here_NEVER_COMMIT"

# Google Analytics
NEXT_PUBLIC_GA_ID="G-FPG3HTSN2R"