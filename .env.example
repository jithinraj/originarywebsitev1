# Database (not required for static build)
# DATABASE_URL="postgres://username:password@localhost:5432/originary"

# NextAuth.js (not required for static build)
# NEXTAUTH_URL="https://your-domain.vercel.app"
# NEXTAUTH_SECRET="your-super-secret-jwt-secret-here-generate-a-long-random-string"

# Stripe Configuration
# Get these from https://dashboard.stripe.com/apikeys
# IMPORTANT: Only publishable key goes in frontend (public)
# SECRET keys must NEVER be exposed in client code

# Stripe Publishable Key (Public - safe to use in frontend)
# Format: pk_live_XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY="pk_live_YOUR_PUBLISHABLE_KEY_HERE"

# Stripe Secret Key (Private - server-side only!)
# WARNING: Never expose this in frontend code
# STRIPE_SECRET_KEY="sk_live_YOUR_SECRET_KEY_HERE"

# Stripe Webhook Secret (for webhook signature verification)
# STRIPE_WEBHOOK_SECRET="whsec_YOUR_WEBHOOK_SECRET_HERE"

# Stripe Price IDs (create these in Stripe dashboard)
# PRICE_DEV1="price_XXXXXXXXXXXXX"  # $1 developer activation
# PRICE_PRO="price_XXXXXXXXXXXXX"   # Professional subscription

# Site URL (for Stripe success/cancel redirects)
NEXT_PUBLIC_SITE_URL="https://www.originary.xyz"

# Google Analytics
NEXT_PUBLIC_GA_ID="G-FPG3HTSN2R"