# Originary CLI

Verify PEAC policy files and check compliance.

## Usage

```bash
# Show version
./originary --version

# Check for peac.txt in current directory
./originary verify
```

## Installation

Extract the archive and add the `bin` directory to your PATH (macOS/Linux) or run `originary.bat` directly (Windows).

## Requirements

None. This is a standalone utility.